<!-- sub-pages/apply/list.wxml -->
<view class="page">
  <view class="header-bg">
    <view class="search-box">
      <icon type="search" size="16" color="#cbd5e1" />
      <input class="search-input" placeholder="搜索政策/通知/模板" />
    </view>
  </view>

  <view class="content">
    <view class="panel">
      <view class="panel-head">
        <text class="panel-title">我的证明/申请</text>
        <text class="panel-sort">按时间从远及近</text>
      </view>

      <view class="record-list">
        <view
          wx:for="{{applyList}}"
          wx:key="id"
          class="record-item"
          bindtap="goToDetail"
          data-id="{{item.id}}"
        >
          <view class="record-left">
            <view class="record-icon">{{item.iconText}}</view>
            <view class="record-main">
              <text class="record-title">{{item.title}}</text>
              <text class="status-pill status-{{item.statusClass}}">{{item.status}}</text>
            </view>
          </view>
          <text class="record-time">申请时间：{{item.time}}</text>
        </view>
      </view>
    </view>
  </view>
</view>
